const mongoose = require('mongoose');

const conversionratesresponseSchema = new mongoose.Schema({
    // Schema vazio, adicione propriedades conforme necessário
})

module.exports = mongoose.model('ConversionRatesResponse', conversionratesresponseSchema)
